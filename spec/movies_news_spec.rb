require 'spec_helper'

describe MoviesNews do
  it 'has a version number' do
    expect(MoviesNews::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
